---
import type { NewsItem } from "../data/news";
interface Props { items: NewsItem[] }
const { items } = Astro.props;
---

<ol class="news-list">
  {items.map((item) => (
    <li><time datetime={item.date.replace(".", "-")}>{item.date}</time><span>{item.text}</span></li>
  ))}
</ol>
